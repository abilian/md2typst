image: alpine/latest
packages:
  - uv
  - gcc
  - python3-dev

secrets:
  - b88a4c2c-944c-42a6-9fbb-7483049c458a

sources:
  - git@git.sr.ht:~sfermigier/md2typst

tasks:
  - install: |
      cd md2typst
      uv sync
  - test: |
      cd md2typst
      uv run pytest -q --tb=short
  - lint: |
      cd md2typst
      uv run ruff check
      uv run ty check src
  - nox: |
      cd md2typst
      uvx nox

triggers:
  - action: email
    condition: failure
    to: sf@fermigier.com
